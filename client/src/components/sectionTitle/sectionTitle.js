import './index.css'

const SectionTitle = ({title}) => {
    return ( 
<h2 className="section-title m-5 text-center" style={{fontFamily:'oswald'}}>
    {title}
</h2>

     );
}
 
export default SectionTitle;